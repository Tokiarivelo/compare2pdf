<Window x:Class="Compare2pdf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Compare2pdf"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <TextBox x:Name="TextBox1" TextWrapping="Wrap" AcceptsReturn="True" ScrollViewer.CanContentScroll="True" BorderBrush="Black" BorderThickness="1"/>
        <TextBox Grid.Column="1" x:Name="TextBox2" TextWrapping="Wrap" AcceptsReturn="True" ScrollViewer.CanContentScroll="True" BorderBrush="Black" BorderThickness="1"/>
    </Grid>
</Window>
